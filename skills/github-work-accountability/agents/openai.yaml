interface:
  display_name: "GitHub Work Accountability"
  short_description: "Keep GitHub work current during execution"
  default_prompt: "Use $github-work-accountability to detect managed work for this repository, perform the requested work, and keep its GitHub lifecycle state accurate."
policy:
  allow_implicit_invocation: true
